server:
  port: 8083

spring:
  application:
    name: movimiento-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bancobd}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: movimiento_schema
    open-in-view: false

  jackson:
    serialization:
      write-dates-as-timestamps: false

services:
  cuenta-service:
    url: http://${CUENTA_SERVICE_HOST:localhost}:${CUENTA_SERVICE_PORT:8082}
  cliente-service:
    url: http://${CLIENTE_SERVICE_HOST:localhost}:${CLIENTE_SERVICE_PORT:8081}

management:
  endpoints:
    web:
      exposure:
        include: health, info
